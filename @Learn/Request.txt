/**
 * Laravel Request Object Structure (Conceptual Simulation)
 * تم تمثيله ككائن نصي لسهولة الفهم
 */

const LaravelRequest = {
    // 1. البيانات القادمة من المستخدم (الأكثر أهمية لك كـ Developer)
    "attributes": {
        "query": { /* البيانات القادمة في الرابط بعد علامة ? (GET) */ },
        "request": { /* البيانات القادمة من فورم (POST) */ },
        "files": { /* الملفات المرفوعة كـ UploadedFile objects */ },
        "cookies": { /* ملفات تعريف الارتباط المرسلة مع الطلب */ }
    },

    // 2. معلومات المسار والروابط
    "server": {
        "REQUEST_URI": "/admin/hospital/doctor/save",
        "REQUEST_METHOD": "POST",
        "REMOTE_ADDR": "192.168.1.1", // IP المستخدم
        "HTTP_USER_AGENT": "Mozilla/5.0...", // معلومات المتصفح
        "PATH_INFO": "/admin/hospital/doctor/save"
    },

    // 3. الترويسات (Headers)
    "headers": {
        "host": "localhost:8000",
        "connection": "keep-alive",
        "accept": "application/json", // هل المستخدم يتوقع رد JSON؟
        "authorization": "Bearer ...", // توكن تسجيل الدخول (إذا وجد)
        "referer": "http://localhost:8000/admin/dashboard"
    },

    // 4. معلومات الجلسة والمصادقة (تتم إضافتها عبر Middleware)
    "session": {
        "id": "abc123xyz...",
        "last_activity": 1715332000,
        "old_input": { /* البيانات السابقة في حال حدوث خطأ Validation */ }
    },

    // 5. حالة الطلب الحالية (Internal State)
    "format": "html",
    "content_type": "application/x-www-form-urlencoded",
    "locale": "ar", // لغة الموقع الحالية
};

/**
 * أهم الأساليب (Methods) الملحقة بهذا الكائن للوصول للبيانات:
 * 
 * $request->all();         // تعيد كل ما في 'query' و 'request'
 * $request->input('key');  // تبحث عن المفتاح في 'request' ثم 'query'
 * $request->file('img');   // تدخل إلى قسم 'files'
 * $request->header('key'); // تدخل إلى قسم 'headers'
 * $request->ip();          // تجلب 'REMOTE_ADDR' من 'server'
 */