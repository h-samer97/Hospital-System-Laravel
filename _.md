دالة: جلب_كشف_حساب_المريض (المريض)
--------------------------------------------------
1. ابحث في الذاكرة المؤقتة (Cache) عن "كشف_حساب_المريض_[رقم_المريض]":
   - إذا كان موجوداً في الذاكرة -> أرجع النتيجة المجهزة فوراً دون مجادلة قاعدة البيانات.
   - إذا لم يكن موجوداً -> قم بتحديث البيانات عبر الخطوات التالية:

2. [جلب البيانات من قاعدة البيانات]:
   - هات جميع الحركات المالية الخاصة بـ (رقم_المريض).
   - احلب معها البيانات المرتبطة بها مسبقاً (الفواتير، سندات القبض، سندات الصرف) لمنع تكرار الاستعلامات (Eager Loading).
   - رتب الحركات زمنياً حسب (التاريخ)، ثم حسب (الرقم التعريفي ID) لضمان تسلسل الدقة.

3. [حساب الرصيد المتراكم]:
   - ابدأ بـ متغير (الرصيد_المتراكم) = 0

4. [التكرار والترتيب لكل حركة مالية في القائمة]:
   أ) حدد نوع الحركة والوصف:
      - إذا كانت مرتبطة بفاتورة   <- الوصف = "اسم الخدمة"، نوع الحركة = "فاتورة"
      - إذا كانت مرتبطة بسند قبض  <- الوصف = "وصف سند القبض"، نوع الحركة = "قبض"
      - إذا كانت مرتبطة بسند صرف <- الوصف = "وصف سند الصرف"، نوع الحركة = "صرف"
      - خلاف ذلك                 <- الوصف = "—"، نوع الحركة = "أخرى"

   ب) احسب الرصيد المتراكم الجديد:
      - الرصيد_المتراكم = الرصيد_المتراكم + (مبلغ المدين - مبلغ الدائن)

   ج) شكّل السطر المالي (Record) بالشكل النهائي:
      - الحقول: (الرقم، التاريخ بصيغة سنة-شهر-يوم، الوصف، نوع الحركة، قيمة المدين، قيمة الدائن، الرصيد المتراكم، وهل المريض مدين؟)

5. خزن هذه القائمة النهائية في الذاكرة المؤقتة (Cache) لاستخدامها مستقبلاً، ثم أرجع القائمة.